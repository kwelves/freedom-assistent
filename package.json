{
  "name": "freedom-helper-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/schedule-fix.js && node --check src/daily-gemini.js",
    "build": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "test": "node --test test/*.test.js",
    "test:live": "node --test test/live/spadna.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
